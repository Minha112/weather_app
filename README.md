# Simple Weather App (Python CLI)

A beginner-friendly command-line weather application built using Python.

## Features
- Fetches temperature, humidity, and weather description.
- Uses OpenWeatherMap API.

## How to Run
1. Install requirements:
```
pip install -r requirements.txt
```
2. Add your API key in config.py
3. Run:
```
python main.py
```
